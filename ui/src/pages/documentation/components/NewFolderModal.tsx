import { Modal } from '@components'

export const NewFolderModal = () => {
    return (
        <Modal
            id={'new-folder-modal'}
            header={'New Folder'}
            body={<></>}
            footer={<></>}
        />
    )
}
