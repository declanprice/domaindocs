import { Modal } from '@components'

export const EditServiceSummaryModal = () => {
    return (
        <Modal
            id={'edit-service-summary-modal'}
            header={'Edit Service Summary'}
            body={<></>}
            footer={<></>}
        />
    )
}
