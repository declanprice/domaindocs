import { Modal } from '@components'

export const EditSubDomainSummaryModal = () => {
    return (
        <Modal
            id={'edit-sub-domain-summary-modal'}
            header={'Edit Sub Domain Summary'}
            body={<></>}
            footer={<></>}
        />
    )
}
