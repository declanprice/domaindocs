// import * as cdk from 'aws-cdk-shared-lib';
// import { Template } from 'aws-cdk-shared-lib/assertions';
// import * as Cdk from '../shared-lib/cdk-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in shared-lib/domaindocs.stack.ts
test('SQS Queue Created', () => {
    //   const app = new cdk.App();
    //     // WHEN
    //   const stack = new Cdk.DomaindocsStack(app, 'MyTestStack');
    //     // THEN
    //   const template = Template.fromStack(stack);
    //   template.hasResourceProperties('AWS::SQS::Queue', {
    //     VisibilityTimeout: 300
    //   });
})
