import { Flex } from '@chakra-ui/react';
import React from 'react';

export const TicketDeskBrowsePage = () => {
    return (
        <Flex width={'100%'} p={8}>
            browse
        </Flex>
    );
};
