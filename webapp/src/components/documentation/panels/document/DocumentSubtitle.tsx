import { Heading } from '@chakra-ui/react';

export const DocumentSubtitle = () => {
    return (
        <Heading size={'md'} fontWeight={'normal'} color={'gray.800'}>
            optional document subtitle
        </Heading>
    );
};
